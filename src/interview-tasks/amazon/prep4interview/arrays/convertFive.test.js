import { convertFive } from './convertFive'

describe('Interview: Amazon. Arrays. convertFive', () => {
  test('convertFive should work', () => {
    expect(convertFive(1004)).toBe(1554)
    expect(convertFive(121)).toBe(121)
  })
})
