import { ProxyClass } from './proxy-class'

describe('ProxyClass', () => {
  // it('should handle invocations', () => {
  //   let proxyClass = new ProxyClass()
  //   expect(proxyClass.method()).toBe('method')
  // }))
  it.todo('should handle invocations')
})
