/**
 * 2392. Build a Matrix With Conditions
 */
/** TODO implement task */
export function buildMatrix(k: number, rowConds: number[][], colConds: number[][]): number[][] {
  const res = Array.from({ length: k }, () => Array(k).fill(0))

  return res
}
