import { subarraySum } from './0560-subarraySum'

it('should work 1', () => {
  expect(subarraySum([1, 1, 1], 2)).toBe(2)
})

it('should work 2', () => {
  expect(subarraySum([1, 2, 3], 3)).toBe(2)
})
